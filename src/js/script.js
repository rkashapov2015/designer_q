
designerQ.init({
    selector: '#main',
    mode: 'constructor'
}
);

/*sendData('http://rest_al/v1/questions/template', null, function (data) {
    designerQ.init({
            selector: '#main',
            mode: 'constructor',
            dataTemplate: data
        }
    );
});*/